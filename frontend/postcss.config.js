/** @type {import('postcss-load-config').Config} */
module.exports = {
  plugins: {
    // use the new package name here:
    '@tailwindcss/postcss': {},
    // keep autoprefixer
    autoprefixer: {},
  },
}